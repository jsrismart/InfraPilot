# Real-Time Cloud Pricing Configuration

# ===== AWS CONFIGURATION =====
# AWS Pricing API requires AWS credentials configured
# Set AWS credentials via environment variables or AWS CLI
# export AWS_ACCESS_KEY_ID="your-access-key"
# export AWS_SECRET_ACCESS_KEY="your-secret-key"
AWS_REGION=us-east-1

# ===== AZURE CONFIGURATION =====
# Azure Pricing API requires Azure subscription
# Use Azure CLI: az login
# Or set credentials:
AZURE_SUBSCRIPTION_ID=your-subscription-id

# ===== GCP CONFIGURATION =====
# GCP Pricing API requires service account credentials
# Download service account JSON from GCP Console
# Set path to credentials file:
GCP_PROJECT_ID=your-project-id
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# ===== PRICING CACHE CONFIGURATION =====
# Cache pricing data locally to reduce API calls
PRICING_CACHE_TTL_HOURS=24
PRICING_CACHE_DIR=./pricing_cache

# ===== FALLBACK CONFIGURATION =====
# Use static pricing if real-time APIs fail
USE_FALLBACK_PRICING=true

# ===== API RATE LIMITING =====
PRICING_API_RATE_LIMIT=60

# ===== CURRENCY =====
DEFAULT_CURRENCY=USD
